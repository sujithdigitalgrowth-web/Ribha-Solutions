# API URL for login/register (PHP API)
# Leave empty to use localStorage only
# Example for production: https://yourdomain.com/upwork-api
# Example for local: http://localhost:8080 (when running: cd upwork-api && php -S localhost:8080)
VITE_API_URL=https://app.ribhasolutions.com/api/
